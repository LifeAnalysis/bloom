schema = "bloom.triad-compatibility/1"
protocol_major = 1
protocol_minor_min = 0
protocol_minor_max = 0
downgrade = "forbidden"
adjacent_versions_supported = false

[[bundles]]
machine = "0.1.1"
broker = "0.1.0"
signer = "0.1.0"
backends = ["local", "aws-kms"]
algorithms = ["ed25519", "secp256k1", "p256"]

[unsupported]
behavior = "fail_closed"
reason = "v1 ships current/current only; no adjacent service combination is supported"
